# Where the Go process listens, and the secret both sides check. The renderer
# reads these in development (vite) and in production (the built server).
RSC_BACKEND=http://127.0.0.1:8080
RSC_HOST_CALL_SECRET=change-me-before-anyone-can-reach-this
