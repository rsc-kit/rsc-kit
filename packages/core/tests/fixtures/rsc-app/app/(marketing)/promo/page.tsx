// A route group: (marketing) organises the files and contributes no url
// segment, so this answers /promo rather than /(marketing)/promo.
export default function PromoPage() {
  return <main id="promo">Promo</main>
}
